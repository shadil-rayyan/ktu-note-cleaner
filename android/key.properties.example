# Example signing config for Android release builds
# Copy this file to android/key.properties and fill in actual values.
# Keep your real android/key.properties out of git (ensure it's gitignored).

storeFile=android/app/keystore.jks
storePassword=your-store-password
keyAlias=your-key-alias
keyPassword=your-key-password
